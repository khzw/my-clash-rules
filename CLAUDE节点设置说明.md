# Claude 节点设置说明

本文适用于 `configdns_v8_Claude.yaml`。这份配置使用三个策略组控制 Claude 的出口：

- `🧠 Claude`：选择最终使用哪一类线路。
- `🛫 Claude 中转`：选择连接静态 ISP 前是否经过机场，以及经过哪个机场节点。
- `🏠 Claude 落地`：选择最终对外显示的静态 ISP。

## 第一次使用

首次载入配置后，按下面顺序设置：

1. 在 `🧠 Claude` 中选择 `🏠 Claude 落地`。
2. 在 `🏠 Claude 落地` 中选择一个静态 ISP；默认建议使用 `静态ISP-2`。
3. 打开 `🛫 Claude 中转`，选择一个机场节点，或者选择 `➡️ 直连落地`。

`🛫 Claude 中转`初始选中 `REJECT`。这是为了防止尚未选择线路时自动使用错误的机场。保持
`REJECT` 时，走静态 ISP 的 Claude 流量会被阻断。

完成一次选择后，Mihomo 会通过 `store-selected` 记住各组设置，通常重启后不需要重新选择。

## 常用设置

| 想要的效果 | `🧠 Claude` | `🛫 Claude 中转` | `🏠 Claude 落地` |
|---|---|---|---|
| 机场中转后使用家宽 IP | `🏠 Claude 落地` | 选择任意机场节点 | 选择 ISP-1/2/3 |
| 不经过机场，直接连接家宽 | `🏠 Claude 落地` | `➡️ 直连落地` | 选择 ISP-1/2/3 |
| 普通机场节点直接作为出口 | 选择默认、地区、自动、其他或全部节点组 | 不参与 | 不参与 |
| Claude 完全直连 | `直连` | 不参与 | 不参与 |

### 推荐用法：机场加静态 ISP

这是最常用的链式路径：

```text
设备 → 机场节点 → 静态 ISP → Claude
```

设置方法：

1. `🧠 Claude` 选择 `🏠 Claude 落地`。
2. `🏠 Claude 落地`选择希望对外显示的静态 ISP。
3. `🛫 Claude 中转`选择用于连接该 ISP 的机场节点。

Claude 和 IP 检测网站看到的是静态 ISP 的地址，不是前面的机场地址。更换中转机场不会改变最终
出口 IP；更换落地 ISP 才会改变最终出口 IP。

### 直接连接静态 ISP

路径如下：

```text
设备 → 静态 ISP → Claude
```

设置方法：

1. `🧠 Claude` 选择 `🏠 Claude 落地`。
2. `🛫 Claude 中转`选择 `➡️ 直连落地`。
3. `🏠 Claude 落地`选择 ISP-1/2/3。

这里的 `➡️ 直连落地`只表示“不通过机场连接静态 ISP”。Claude 最终仍通过静态 ISP 访问，
并不是完全不使用代理。

### 普通机场节点直接作为出口

如果不想使用静态 ISP，可以在 `🧠 Claude` 中直接选择：

- `🚀 默认代理`
- 任一地区故障转移或自动选择组
- `♻️ 自动选择`
- `🌍 其他节点`
- `🌐 全部节点`

此时 Claude 直接使用所选机场节点，`🛫 Claude 中转`和 `🏠 Claude 落地`的当前选择不会参与连接。

## 检查是否设置成功

配置中的 IP 检测网站会跟随 `🧠 Claude`策略。设置完成后，可以访问 `ipinfo.io` 或
`browserleaks.com` 检查公网出口。

- 使用“机场加静态 ISP”时，应显示所选静态 ISP 的地址。
- 使用“直接连接静态 ISP”时，也应显示所选静态 ISP 的地址。
- 使用普通机场节点直出时，应显示相应机场节点的地址。
- 切换中转机场后公网 IP 不应改变；切换落地 ISP 后公网 IP 应改变。

## 常见问题

### Claude 完全打不开

先检查 `🛫 Claude 中转`是否仍为 `REJECT`。如果是，请选择一个机场节点或
`➡️ 直连落地`。

### 链式代理无法连接

把 `🛫 Claude 中转`临时改为 `➡️ 直连落地`：

- 改完可以使用：问题通常在所选机场节点或机场到静态 ISP 的链路。
- 改完仍不能使用：尝试更换 `🏠 Claude 落地`中的静态 ISP，并检查 ISP 账号是否有效。

### 想更换中转机场但保持同一个出口 IP

只修改 `🛫 Claude 中转`，不要修改 `🏠 Claude 落地`。

### 想更换最终出口 IP

修改 `🏠 Claude 落地`中的 ISP。第一层机场只负责中转，不决定 Claude 看到的最终地址。

### 机场订阅更新后，原节点失效

先刷新对应的 proxy provider，再进入 `🛫 Claude 中转`重新选择节点。保存的旧节点名称如果已从
订阅中消失，需要手动改选。

## 安全注意事项

- 真实机场订阅和静态 ISP 凭据只能保存在 `with_subscription/configdns_v8_Claude.yaml`。
- 不要把私有配置、订阅 URL、ISP 服务器、用户名或密码复制到项目根目录或提交到 Git。
- 根目录的 `configdns_v8_Claude.yaml` 仅为公开模板，其中必须保持占位值。
